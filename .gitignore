/.phpunit.cache
/bootstrap/ssr
/node_modules
/public/build
/public/hot
/public/storage
/storage/*.key
/storage/pail
/resources/js/actions
/resources/js/routes
/resources/js/wayfinder
/vendor
.DS_Store
.env
.env.backup
.env.production
.phpactor.json
.phpunit.result.cache
Homestead.json
Homestead.yaml
npm-debug.log
yarn-error.log
/auth.json
/.fleet
/.idea
/.nova
/.vscode
/.zed


# Claudify — personal & runtime files
CLAUDE.local.md
/.claude
__NEEDS_ONBOARD

# Personal workflow & planning artefacts — not part of the product
/openspec
/Daily Notes
/Task Board.md
/Scratchpad.md

# Legacy SQLite file — project runs on Postgres via Docker now
/database/*.sqlite*

# Entrypoint flock file — runtime, per-container
/storage/.boot.lock

# Workflows are re-enabled as of commit 6 (CI). The starter's tests.yml
# and lint.yml were rewritten for the Docker + Postgres stack.
